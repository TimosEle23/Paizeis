# React Native's dependency graph routinely has peer conflicts between optional
# transitive packages (react-server-dom-webpack against a pinned React, for one).
# npm's strict peer resolution deadlocks on them; Expo's own tooling assumes
# this setting in monorepos.
legacy-peer-deps=true
